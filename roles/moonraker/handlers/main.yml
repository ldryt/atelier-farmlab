---
- name: reload systemd
  become: true
  systemd:
    daemon_reload: yes

- name: restart moonraker
  become: true
  systemd:
    name: "moonraker@{{ item.name }}"
    state: restarted
  loop: "{{ printers }}"
